<p id="notice"><%= notice %></p>

<div class="form-group">
  <label>Name: </label>
  <label><%= @event.name %></label>
</div>
<div class="form-group">
  <label>Date: </label>
  <label><%= @event.date %></label>
</div>
<div class="form-group">
  <label>Visited:</label>
  <label><%= @event.visited_flag ? 'Yes' : 'No' %></label>
</div>

<%= link_to 'Edit', edit_event_path(@event) %> |
<%= link_to 'Back', events_path %>
